import React from 'react';
import {StyleSheet} from 'react-native';
import appStyle from "../../styles/StyleSheet";
const FishingLocation = ({navigation}) => {
    return(
        <>

        </>
    )
}

const styles = StyleSheet.create({});

export default FishingLocation;
